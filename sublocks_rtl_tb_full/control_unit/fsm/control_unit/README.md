# AI_BYTE Control Unit (compute-only)

**RTL:** `ai_byte_control_unit.v`  
**Cocotb:** `test_control_unit.py` — [VERIFICATION.md](VERIFICATION.md)

Sequences compute only (no buffer data/addr). MMIF owns CPU buffer path.

```bash
cd control_unit && make
```
